FastApi,SQLalchemy,SQLLITE,xlswriter,PythonDocx,SQLModel,Matplotlib,BCrypt,Pandas #Библиотеки
#Таблица Справочник льгот,Льготники,Логи событий,регистрация справок по льготным категориям,пользователи.
#Endpoint 
Льготы корректировка и пополнение Privilages
Информация о льготниках добавлять,просматривать и редактировать информацию, Citizens
логировать действия пользователей EventLog
возвращать фильтрованный журнал событий по дате,выгрузка в Excel,
отдавать WORD/Excel и уметь фильтровать по категориям, возвращает диаграммы в Excel
регистрация справок по льготникам, Certificates
регистрация пользователей. Users



git pull-Синхронизация локальной ветки с удаленной

git add-Добавляет в гит
git commit-Фиксирует изменения
git push-Пуш на сервер

git fetch-Синхронизация всех веток
git branch-Показать все ветки
git checkout-Смена ветки
Создать ветку-git branch имя ветки
git checkout -b название ветки
git merge-Объединяет ветки

alembic revision --autogenerate -m комментарий к миграции
alembic upgrade head применить миграции

pip freeze > requirements.txt зависимости из виртуалки добавить в requirements
pip install -r requirements.txt
pip uninstall название 

Порядок запуска сервака
1.Активировать виртуалку
2.source venv/Scripts/activate
3.Переход в app
4.Запуск uvicorn main:app --reload сервак запускает
